// Challenge 38
// Set result = 15
// Only write inside the flatiron object

var numberOfHoustonCodeoStudents = function(school, cohortNumber){
    return school.cohorts[0].numberOfStudetns
}

const flatiron = {
    // ?
}

var result = numberOfStudents(flatiron, 0) 