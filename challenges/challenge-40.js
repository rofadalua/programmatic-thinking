// Challenge 40
// Set result = 15
// Only write inside the flatiron object

var numberOfStudents = function(schools, cohortNumber){
    return schools.flatiron.cohorts[cohortNumber].numberOfStudetns
}

const schools = {
     // ?
}

var result = numberOfStudents(schools, 1) 