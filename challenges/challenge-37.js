// Challenge 37
// Set result = 15
// Only write inside the flatiron object

var numberOfHoustonCodeoStudents = function(school){
    return school.cohorts[0].numberOfStudetns
}

const flatiron = {
    // ?
}

var result = numberOfHoustonCodeoStudents(flatiron) 