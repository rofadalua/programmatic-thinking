// Challenge 39
// Set result = 15
// Only write inside the flatiron object

var numberOfStudents = function(school, cohortNumber){
    return school.cohorts[cohortNumber].numberOfStudetns
}

const flatiron = {
    // ?
}

var result = numberOfStudents(flatiron, 1) 