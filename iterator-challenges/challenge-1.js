// Challenge 1
// Make the code below run once for every element in the array 
// (it should run three times)

var array = [ 1, 2, 3];
array.forEach(function(element) {

    console.log('I should run 3 times') 
})